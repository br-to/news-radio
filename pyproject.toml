[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "news-radio"
version = "0.3.0"
description = "Daily news audio generator using NotebookLM CLI"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
news-radio = "news_radio.main:main"
